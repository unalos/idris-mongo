Idris bindings to MongoDB C connector
=====================================

The idrisMongo.bin executable expects a mongodb URI as first argument. For instance mongodb://localhost if you have mongodb running locally.